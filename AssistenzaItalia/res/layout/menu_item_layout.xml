<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="5dp"
    android:background="@drawable/menu_item_selector">

    <ImageView
        android:id="@+id/menu_icon"
        android:layout_width="25dp"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/menu_title"
        android:layout_alignParentLeft="true"
        android:layout_alignTop="@+id/menu_title"
        android:layout_toLeftOf="@+id/menu_title" />
    
    <TextView android:id="@+id/menu_counter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/menu_counter_bg"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="8dp"
        android:textColor="#c5c4c4"/>

    <TextView
        android:id="@+id/menu_title"
        android:layout_width="200dp"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:gravity="center_vertical"
        android:minHeight="?android:attr/listPreferredItemHeightSmall"
        android:paddingRight="40dp"
        android:textColor="#fff"
        android:textSize="18sp" />

</RelativeLayout>